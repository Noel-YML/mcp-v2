{
  "name": "ariel-mcp-revenue-view",
  "private": true,
  "version": "1.0.0",
  "description": "MCP App View for get_performance_digest (ui://ariel/revenue-performance) - presentation-only, no analytics, no network calls.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit",
    "test:unit": "node --test test/format.test.mjs test/csp.test.mjs",
    "test:browser": "node test/view.browser.test.mjs",
    "test": "npm run typecheck && npm run test:unit && npm run test:browser"
  },
  "devDependencies": {
    "@modelcontextprotocol/ext-apps": "1.7.5",
    "esbuild": "^0.28.2",
    "puppeteer-core": "^25.9.0",
    "typescript": "^5.6.0"
  }
}
